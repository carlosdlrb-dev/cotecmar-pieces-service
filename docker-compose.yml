services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pieces-service
    ports:
      - "8001:80"
    environment:
      APP_NAME: Laravel
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: http://localhost:8001
      DB_CONNECTION: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: pieces_service
      DB_USERNAME: pieces_user
      DB_PASSWORD: pieces_password
      JWT_SECRET: V9FutA6STy01u4eiS5xZwzmrEoXAsIa4HRHCg78QMHW8ifXFupTgIToJLWIkJwcs
      JWT_TTL: 60
      JWT_ALGO: HS256
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - pieces-network

  mysql:
    image: mysql:8.0
    container_name: pieces-mysql
    environment:
      MYSQL_DATABASE: pieces_service
      MYSQL_USER: pieces_user
      MYSQL_PASSWORD: pieces_password
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - "3308:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - pieces-network

volumes:
  mysql-data:

networks:
  pieces-network:
    driver: bridge
